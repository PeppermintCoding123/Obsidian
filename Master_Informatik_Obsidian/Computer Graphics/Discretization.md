= Stubdevision into pixels
# Sampling
= sample at center
$$C_{i,j} = f(x_i, y_j)$$
# Filtering
= average over Pixel
$$C_{i,j,} = \int_{[x_i-\frac{w}{2}, x_i + \frac{w}{2}]} \int_{[y_j-\frac{h}{2}, y_j+\frac{h}{2}]}f(x, y)dxdy$$