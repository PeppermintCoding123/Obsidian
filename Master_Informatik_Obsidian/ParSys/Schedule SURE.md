Given unfolded [[RDG]] of [[SURE]]
- if $a_w(I_1)$ depends on $a_v(I_2)$
- => $a_v(I_2)$ before $a_w(I_1)$

# Schedule:
assign function $T$ to each vector:
$T(v,I_2) < T(w, I_1)$

# Computable:
if $\exists$ schedule $\Longrightarrow$ SURE = computable 

For $\mathcal{I}= \{I\in \mathbb{Z}^n < I \geq 0\}$ & 
G $\nexists$ <span style="color:rgb(0, 176, 240)">cycle of nonpositive weight</span>
$\Longrightarrow$ SURE is computable

[[Detecting nonpositive cycles]]