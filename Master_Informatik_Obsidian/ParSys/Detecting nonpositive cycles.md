$Q(D) = \{q \in \mathbb{Z}^m | q \geq 0 \wedge q \neq 0 \wedge Dq = 0\}$
$m = |E|$ number of Edges in G
$q$ = path vector denotes how often each edge is traversed

$T(D)\neq \emptyset \Leftrightarrow Q(D)= \emptyset$
$D$ = Matrix of data dependancies
$T(D) = {\lambda \in \mathbb{Z}^n | \lambda D \geq 1}$
Each vector in $T(D)$ defines linear scedule $\lambda I$
