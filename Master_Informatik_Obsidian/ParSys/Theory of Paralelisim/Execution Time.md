$$T = I_c(p+m\cdot k)\tau$$
$I_c$ = Instruction count
$p$ = Time for instruction decode + execute
$m$ = Average \# memory accesses per instructions
$k$ = Ratio memory cycle to processor cycle
$\tau$ = Clock periods