$\langle \sum^f, \sum^p\rangle$
$\sum^f := \bigcup_{k\in\mathbb{N}} \sum_k^f$ function constants
- $h \in \sum_k^f$ => h takes k objects & returns object
$\sum^p := \bigcup_{k \in\mathbb{N}} \sum_k^p$
- $q \in \sum_k^p$ => $q$ takes k objects and returns boolean

[[First Order Logic PL1]]
[[AI1-overview.canvas]]