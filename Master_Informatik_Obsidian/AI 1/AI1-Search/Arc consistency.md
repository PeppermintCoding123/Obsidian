$C_{uv}\neq C \vee \forall d\in D_u \exists d'\in D_v: (d, d')\in C_{uv}$=> variable $u\in V$ is arc consistent relative to another variable $v\in V$

Constraint network is arc consistent if every variable is arc consistent to every other variable

=> alle zur auswahl stehenden variablen sind in kombination mit den anderen Variablen sinvoll/funktionierten.

[[AI1-overview.canvas]]
[[Search Problems Math]]