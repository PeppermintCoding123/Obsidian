= [[Inference]] in constraint Networks
=> get additional constraints that follow from existing / satirfied in all solutions
=> $\gamma$ get strictly tighter equivalent constraint networks $\gamma '$

# Equivalent $\gamma \equiv \gamma '$
= have same solution

# tighter $\gamma ' \sqsubseteq \gamma$
$\forall v \in V: D'_v \subseteq D_v$
$\forall u\neq v \in V and C'_{uv} \in C': C'_{uv}\neq C \vee C'_{uv}\subset C_{uv}$
= add more constraints or change constraints to be tighter or to shrink the domain of consistent assignments