NumEigNumerischeEigenwerte 
[[7.2 Krylov subspace]]
[[Numerical Methods for Eigenvalue Problems - Steffen Börn, Christian Mehl]]


compute Ritz Pairs of $\mathcal{X} \subseteq \mathbb{F}^{\; n}$ compute [[orthonomal basis]] of $\mathcal{X}$. 
-> need orthonormal basis of Krylov Subspace $\mathcal{K}_k(A,x) := span(x, Ax, A^2x, ... , A^{k-1}x)$ for many k

suppose $\mathcal{K}_k(A,x) = k$  & orthonormal basis of $\mathcal{K}_k(A,x)$ is $q_1, ..., q_k$
for $\mathcal{K}_{k+1}(A,x) = k+1$  :    $\mathcal{K}_{k+1}(A,x) = span(x, Ax, A^2x, ... , A^{k-1}x ,A^{k}x) = span(q_1, ... , q_k ,A^{k}x)$
=> need to orthonormalize $A^k x$ against $q_1, ..., q_k$ 

produce vectors $v_1, ..., vk$ that: $span(v_1, ..., vk) = span(q_1, ..., q_k)$
[[Gram-Schmidt procedure for vectors v1, .... , vm in F^n]] 

Behaviour:
- numerical behaviour = kindof bad
- roundoff errors
- modify slightly

[[Modified Gram-Schmidt procedure for vectors v1, .... , vm in F^n]] 

- with reorthogonalisation

[[Modified Gram-Schmidt procedure with reorthogonalisation]]